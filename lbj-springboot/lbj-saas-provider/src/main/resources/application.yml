server:
  port: 18081
spring:
  application:
    name: lbj-saas-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

# 配置日志级别
logging:
  level:
    #    root: info
    root: info
  pattern:
    file: ./data/log/${spring.application.name}.log




